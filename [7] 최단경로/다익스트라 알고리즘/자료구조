# 스택
    - 가장 나중에 삽입된 데이터 추출(LIFO)
# 큐
    - 가장 먼저 삽입된 데이터 추출(FIFO)
# 우선 순위 큐
    - 가장 우선 순위가 높은 데이터 추출
    - PirorityQueue, heapq를 사용할 수 있음
    - heapq가 더 빠르므로 사용 권장
    - 우선 순위 큐를 구현할 때, 내부적으로 최소 힙 or 최대 힙을 사용함
        - 최소 힙 사용 : 값이 가장 낮은 데이터가 먼저 삭제
        - 최대 힙 사용 : 갑이 가장 높은 데이터가 먼저 삭제
    - 다익스트라 최단 거리 알고리즘은 비용이 적으 노드를 우선 방문하므로, 최소힙 구조를 사용하면 됨
    - 최소힙을 최대힙처럼 쓰기 위해, 일부러 우선 순위 값에 음수 부호(-)를 붙여 넣었다가, 나중에 큐에서 꺼낸 다음 다시 음수 부포(-)를 붙여서
    원래의 값으로 돌리는 방식도 사용할 수 있음
# 우선 순위 큐 구현 방식
    - 리스트
        - 삽입 : O(1)
        - 삭제 : O(N)
    - 힙
        - 삽입 : O(logN)
        - 삭제 : O(logN)
